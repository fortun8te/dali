# DALI Remotion UI walkthrough

20 seconds, 1280 × 720, 30 fps. Uses screenshots of the actual app and rendered onboarding previews. This is a UI walkthrough, not a recording or proof of physical speaker synchronization.

From this folder:

```sh
npm install
npm run render
```

The composition uses only local assets and system fonts. No music or voice-over is included.
